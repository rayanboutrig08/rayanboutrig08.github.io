---
layout: base.njk
---

<main class="max-w-7xl mx-auto px-6 py-12">
    <header class="text-center mb-16 border-b-4 border-black pb-8">
        <h1 class="text-6xl font-serif font-black uppercase tracking-tighter mb-2">The Digital Journal</h1>
        <p class="text-xl italic text-gray-600">Investigative Journalism & Economic Analysis</p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
        {% for post in collections.blog %}
            <article class="flex flex-col border-b md:border-b-0 md:border-r border-gray-200 pr-0 md:pr-6 last:border-0">
                {% if post.data.thumbnail %}
                    <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" class="w-full h-48 object-cover mb-4">
                {% endif %}

                <h2 class="text-2xl font-serif font-bold leading-tight mb-3">
                    <a href="{{ post.url }}" class="hover:text-blue-900 transition-colors">
                        {{ post.data.title }}
                    </a>
                </h2>

                <p class="text-gray-700 text-sm mb-4 line-clamp-3">
                    {{ post.templateContent | striptags | truncate(150) }}
                </p>

                <div class="mt-auto pt-4 border-t border-gray-100">
                    <p class="text-xs font-bold uppercase tracking-widest text-gray-500">By {{ post.data.author }}</p>
                    <p class="text-xs text-gray-400">{{ post.date | date }}</p>
                </div>
            </article>
        {% else %}
            <div class="col-span-full bg-gray-50 border-2 border-dashed border-gray-300 p-20 text-center">
                <h3 class="text-xl font-bold text-gray-400">No Articles Found</h3>
                <p class="text-gray-500 mt-2">The grid is ready! We just need to make sure your posts have the "blog" tag.</p>
            </div>
        {% endfor %}
    </div>
</main>
